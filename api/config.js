// For a separate AI backend, replace this with the full HTTPS endpoint URL.
// Same-origin Vercel deployment works with the default value.
window.MESHDOCTOR_AI_ENDPOINT = window.MESHDOCTOR_AI_ENDPOINT || '/api/ai-correct';
